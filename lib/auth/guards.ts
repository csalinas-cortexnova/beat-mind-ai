/**
 * Auth guards — STUB for Spec 01.
 * Full implementation comes in Spec 02 (Authentication & Authorization).
 */

/**
 * Resolves the current user's gym context.
 * STUB: Always throws until Spec 02 is implemented.
 */
export function requireGymContext(): never {
  throw new Error(
    "requireGymContext() is a stub. Implement in Spec 02 (Authentication & Authorization)."
  );
}
